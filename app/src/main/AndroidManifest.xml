<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@style/Theme.ProgressDay" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher" android:resizeableActivity="true" android:allowBackup="true">
        <activity android:name=".ConfigureActivity" android:exported="true" />
        <receiver android:name=".AgeWidgetProvider" android:exported="true">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/age_in_motion_widget_info" />
        </receiver>
    </application>
</manifest>
